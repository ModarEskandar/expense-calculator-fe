/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    // API_URI: "http://localhost:8080/api",
    API_URI: "http://expensescalculator.ap-1.evennode.com/api",
  },
};

export default nextConfig;
